doctype html
html(lang="ko")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Noom
        link(rel="stylesheet" href="https://unpkg.com/mvp.css")
    body 
        header
            h1 Noom
        main
            #welcome 
                form
                    input(placeholder="room name", reqiured, type="text")
                    button Enter Room
                h4 Open Rooms: 
                ul
            #room 
                h3
                ul 
                form#name
                    input(placeholder="nickname", reqiured, type="text")
                    button Save
                form#msg
                    input(placeholder="message", reqiured, type="text")
                    button Send
        script(src="/socket.io/socket.io.js")
        script(src="/public/js/app.js") 